language: node_js
node_js:
- stable
script:
  - yarn lint
  - yarn test
deploy:
  - provider: script
    script: npm install -g now && now --token=$NOW_TOKEN && now --token=$NOW_TOKEN alias cserdean.me
    on:
      branch: master
  - provider: script
    script: npm install -g now && now --token=$NOW_TOKEN && now --token=$NOW_TOKEN alias dev.cserdean.me
    on:
      branch: dev
notifications:
  email:
    on_failure: always
    on_success: never
